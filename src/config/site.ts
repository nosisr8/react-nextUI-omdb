export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "Next.js + NextUI + OMDb API",
	description: "he OMDb API is a RESTful web service to obtain movie information, all content and images on the site are contributed and maintained by our users.",
	navItems: [
		{
			label: "Inicio",
			href: "/",
		}
	],
	navMenuItems: [
		{
			label: "Inicio",
			href: "/",
		}
	],
	links: {
		github: "https://github.com/nosisr8"
	}
};